spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://127.0.0.1:5432/productd
    username: postgres
    password: 123qweR
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: PRODUCT
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true